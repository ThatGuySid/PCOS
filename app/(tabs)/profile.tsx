import { View, Text } from "react-native";

// Placeholder — replace when you build the Profile page
export default function ProfileScreen() {
  return (
    <View className="flex-1 bg-[#FDF0F2] items-center justify-center">
      <Text className="text-[#C0162C] text-lg font-bold">Profile — coming soon</Text>
    </View>
  );
}
