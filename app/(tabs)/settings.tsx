import { View, Text } from "react-native";

// Placeholder — replace when you build the Settings page
export default function SettingsScreen() {
  return (
    <View className="flex-1 bg-[#FDF0F2] items-center justify-center">
      <Text className="text-[#C0162C] text-lg font-bold">Settings — coming soon</Text>
    </View>
  );
}
