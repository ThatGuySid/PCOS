import { Text, View } from "react-native";

// Placeholder — replace this file when you build the login page
export default function LoginPage() {
  return (
    <View
      style={{
        flex: 1,
        alignItems: "center",
        justifyContent: "center",
        backgroundColor: "#FBF5F5",
      }}
    >
      <Text style={{ color: "#C0162C", fontSize: 20, fontWeight: "700" }}>
        Login — coming next
      </Text>
    </View>
  );
}
